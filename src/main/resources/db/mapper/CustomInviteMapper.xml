<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.techuni.TechUniInviteSystem.db.mapper.CustomInviteMapper">

    <update id="addUsedCountToInvite">
        UPDATE invite
        SET invite.used = invite.used + #{amount}
        WHERE invite.id = #{inviteId}
    </update>
    
    <insert id="insertInvite" parameterType="org.techuni.TechUniInviteSystem.db.entity.base.Invite" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO invite (code, search_id, is_disabled, used, max_used, target_app_id, expires_at)
        VALUES (#{invite.code}, #{invite.searchId}, #{invite.isDisabled}, #{invite.used}, #{invite.maxUsed}, #{invite.targetAppId}, #{invite.expiresAt, jdbcType=TIMESTAMP})
    </insert>

</mapper>